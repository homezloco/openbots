## Summary

<!-- What does this change and why? -->

## Test plan

- [ ] `pnpm typecheck` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm build` passes
- [ ] If this touches routing, credentials, providers, or the write/push/schedule paths: a new case in `apps/api/e2e/run.ts`, and `pnpm --filter @openbots/api test:e2e` passes

## Notes for reviewers

<!-- Anything surprising, tradeoffs, or follow-ups left out of scope -->
