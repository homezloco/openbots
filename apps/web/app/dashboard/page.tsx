/**
 * Placeholder for the Phase 2 chat/playground dashboard (single-agent
 * chat, usage/cost tracking). The hierarchy canvas at /hierarchy is the
 * Phase 1 focus — see PLAN.md.
 */
export default function DashboardPage() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Dashboard</h1>
      <p>Chat/playground view — coming in Phase 2. Start with the Hierarchy tab.</p>
    </div>
  );
}
